<nav>
  <% if (locals.currentUser.isFacebookUser && locals.currentUser.pfpUrl) { %>
    <div class="pfp-img-wrapper">
      <img alt="pfp" class="pfp-img" src="<%=locals.currentUser.pfpUrl%>" />
    </div>
    <% } %>
      <% if (!locals.currentUser.isFacebookUser && locals.currentUser.nonFacebookUserImg &&
        locals.currentUser.nonFacebookUserImg !=="" && locals.currentUser.nonFacebookUserImg.data ) { %>
        <a href="/profile">

          <div class="pfp-img-wrapper">

            <img alt="pfp" class="pfp-img" src="data:image/<%=locals.currentUser.nonFacebookUserImg.contentType%>;base64,  
      <%=locals.currentUser.nonFacebookUserImg.data.toString('base64')%>">
          </div>

        </a>
        <% } %>

          <a href="/profile">
            <div class="mainnavbtn" style="height: 50px; line-height: 30px;">Profile
            </div>
          </a>
          <a href="/home">
            <div class="mainnavbtn" style="height: 50px; line-height: 30px;">Feed</div>
          </a>

          <a href="/users/find-friends">
            <div id="findFriends" class="mainnavbtn" style="height: 50px; line-height: 30px;">Find Friends</div>
          </a>
          
</nav>